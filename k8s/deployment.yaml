apiVersion: batch/v1
kind: Job
metadata:
  name: willscot-automation
  namespace: willscot
  labels:
    app: willscot-automation
spec:
  # Hard cap: kill pod and mark Job failed if not done in 30 min
  activeDeadlineSeconds: 1800
  # No retry — test failures should fail the job immediately, not retry
  backoffLimit: 0
  # Auto-delete Job + pods 10 minutes after completion
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app: willscot-automation
    spec:
      restartPolicy: Never

      containers:
        - name: willscot-automation
          # Local minikube: image loaded via 'minikube image load', never pulled from registry
          # REMOTE: change image to santhipodi/willscot-automation:latest and imagePullPolicy to Always
          image: willscot-automation:latest
          imagePullPolicy: Never

          env:
            - name: TEST_ENV
              value: "Prod"
            - name: BROWSER
              value: "chromium"
            - name: HEADLESS
              value: "false"
            - name: FILTER
              value: ""

          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "4Gi"
              cpu: "2000m"

          # Playwright Chromium needs /dev/shm; map to memory-backed tmpfs
          volumeMounts:
            - name: dshm
              mountPath: /dev/shm

          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              add: ["SYS_ADMIN"]

      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: "2Gi"
